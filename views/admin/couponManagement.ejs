<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Coupon Management</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        :root {
    --primary-bg: #1a1c1e;
    --container-bg: #282c34;
    --text-color: #ecf0f1;
    --primary-accent: #3498db;
    --primary-accent-hover: #2980b9;
    --danger: #e74c3c;
    --danger-hover: #c0392b;
    --success: #2ecc71;
    --warning: #f1c40f;
    --muted: #95a5a6;
    --border-radius: 8px;
}

body {
    background-color: var(--primary-bg);
    font-family: 'Inter', sans-serif;
    color: var(--text-color);
    padding-left: 90px;
}

.dashboard-wrapper {
    min-height: 100vh;
    padding: 2rem;
    padding-left: calc(2rem + 90px);
}

.dashboard-container {
    background-color: var(--container-bg);
    border-radius: var(--border-radius);
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    padding: 1.5rem;
    margin: 0 auto;
}

.page-header {
    background: rgba(255, 255, 255, 0.05);
    border-radius: var(--border-radius);
    padding: 1.25rem;
    margin-bottom: 1.5rem;
}

.header-title {
    color: var(--text-color);
    margin-bottom: 0;
    font-size: 1.8rem;
}

.add-coupon-btn {
    background-color: var(--primary-accent);
    color: var(--text-color);
    border-radius: var(--border-radius);
    padding: 0.75rem 1.25rem;
    border: none;
    transition: all 0.3s ease;
}

.add-coupon-btn:hover {
    background-color: var(--primary-accent-hover);
    color: var(--text-color);
}

.table-responsive-wrapper {
    background: rgba(255, 255, 255, 0.05);
    border-radius: var(--border-radius);
    padding: 1.25rem;
    margin-bottom: 1.5rem;
}

.table {
    color: var(--text-color);
    margin-bottom: 0;
}

.table thead th {
    color: var(--muted);
    border-bottom-color: rgba(255, 255, 255, 0.1);
    padding: 1rem;
    background-color: var(--container-bg);
}

.table td {
    border-bottom-color: rgba(255, 255, 255, 0.1);
    padding: 1rem;
    background-color: var(--container-bg);
    color: var(--text-color);
}

.coupon-code {
    background: rgba(52, 152, 219, 0.1);
    color: var(--primary-accent);
    padding: 0.375rem 0.75rem;
    border-radius: var(--border-radius);
    font-family: monospace;
}

.status-badge {
    padding: 0.375rem 0.75rem;
    border-radius: 2rem;
    font-size: 0.875rem;
}

.status-active {
    background-color: rgba(46, 204, 113, 0.1);
    color: var(--success);
}
.status-inactive{
    background-color: rgba(204, 178, 46, 0.1);
    color: var(--warning);
}

.btn-update {
    background-color: rgba(241, 196, 15, 0.1);
    color: var(--warning);
    border: 1px solid rgba(241, 196, 15, 0.2);
    padding: 0.375rem 0.75rem;
    border-radius: var(--border-radius);
    transition: all 0.3s ease;
}

.btn-update:hover {
    background-color: rgba(241, 196, 15, 0.2);
}

.text-muted {
    color: var(--muted) !important;
}

.pagination {
    margin-bottom: 0;
}

.page-link {
    background-color: rgba(255, 255, 255, 0.05);
    border-color: rgba(255, 255, 255, 0.1);
    color: var(--text-color);
    margin: 0 0.25rem;
    border-radius: var(--border-radius);
}

.page-link:hover {
    background-color: var(--primary-accent);
    border-color: var(--primary-accent);
    color: var(--text-color);
}

.page-item.active .page-link {
    background-color: var(--primary-accent);
    border-color: var(--primary-accent);
}

/* Media Queries */
@media (max-width: 320px) {
    body {
        padding-left: 0;
    }

    .dashboard-container {
        padding: 0.75rem;
    }

    .page-header {
        padding: 1rem;
    }

    .header-title {
        font-size: 1.25rem;
    }

    .add-coupon-btn {
        width: 100%;
        margin-top: 0.75rem;
        padding: 0.5rem;
    }
}

@media (min-width: 321px) and (max-width: 375px) {
    body {
        padding-left: 0;
    }

    .dashboard-container {
        padding: 1rem;
    }

    .table td {
        padding: 0.5rem;
    }
}

@media (min-width: 376px) and (max-width: 425px) {
    body {
        padding-left: 0;
    }

    .table-responsive-wrapper {
        padding: 1rem;
    }

    .status-badge {
        padding: 0.25rem 0.5rem;
    }
}

@media (min-width: 426px) and (max-width: 768px) {
    body {
        padding-left: 0;
    }

    .dashboard-wrapper {
        padding: 1rem;
    }

    .dashboard-container {
        max-width: 720px;
    }
}

@media (min-width: 769px) and (max-width: 1024px) {
    .dashboard-container {
        max-width: 960px;
    }
}

@media (min-width: 1025px) and (max-width: 1440px) {
    .dashboard-container {
        max-width: 1320px;
    }
}

@media (min-width: 1441px) {
    .dashboard-container {
        max-width: 2400px;
    }

    .header-title {
        font-size: 2rem;
    }

    .table {
        font-size: 1.1rem;
    }
}
    </style>
</head>
<body>
   
<!-- Include Sidebar (similar to reference code) -->
<%- include('./layout/asidebar.ejs') %>

<main class="main-content">
    <div class="container-fluid">
       

    <div class="dashboard-wrapper">
        <div class="dashboard-container">
            <div class="page-header">
                <div class="row align-items-center">
                    <div class="col-12 col-md-6">
                        <h1 class="header-title mb-md-0">Coupon Management</h1>
                    </div>
                    <div class="col-12 col-md-6 text-md-end">
                       <a href="/admin/couponManagement/add"><button class="add-coupon-btn">
                        <i class="fas fa-plus me-2"></i>
                        Add New Coupon
                    </button></a> 
                    </div>
                </div>
            </div>

            <p><%= coupon.length %></p>

            <div class="table-responsive-wrapper">
                <div class="table-responsive">
                    
                    <table class="table">
                        <thead>
                            <tr>
                                <th>No</th>
                                <th>Coupon Code</th>
                                <th>Discount</th>
                                <th>Min Purchase</th>
                                <th>Dates</th>
                                <th>Status</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (coupon.length === 0) { %>
                                <tr>
                                    <td colspan="6" class="text-center">No categories found</td>
                                </tr>
                            <% } else { %>
                                <% coupon.forEach((coupon, index) => { %>
                            <tr>
                                <td><%=startIndex + index  %></td>
                                <td><span class="coupon-code"><%= coupon.code  %></span></td>
                                <% if (coupon.discountType==='percentage') { %>
                                    <td><%= coupon.discountValue  %>%</td>
                                    <% } else { %>
                                    <td>$<%= coupon.discountValue  %></td>
                                <% } %>
                                <td><%= coupon.minPurchase  %></td>
                                <td>
                                    <small class="text-muted d-block">Start: <%= new Date(coupon.startDate).toLocaleDateString()  %></small>
                                    <small class="text-muted d-block">End:<%= new Date(coupon.endDate).toLocaleDateString()  %></small>
                                </td>
                                <td>
                                    <% if (coupon.isActive === 'Active') { %>
                                        <span class="status-badge status-active">
                                            Active
                                        </span>
                                    <% } else { %>
                                        <span class="status-badge status-inactive">
                                            Inactive
                                        </span>
                                    <% } %>
                                </td>
                                <td>
                                    <a href="/admin/couponManagement/update/<%= coupon._id  %>">
                                        <button class="btn-update"  >
                                            <i class="fas fa-edit me-1"></i>
                                            Update
                                        </button>
                                    </a>
                                </td>
                            </tr>
                            <% }); %>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>

            <nav aria-label="Page navigation">
                <ul class="pagination justify-content-center">
                    <% if (hasPreviousPage) { %>
                        <li class="page-item">
                            <a class="page-link" href="/admin/couponManagement?page=<%= previousPage %>" aria-label="Previous">
                                <i class="fas fa-chevron-left"></i>
                            </a>
                        </li>
                    <% } %>
            
                    <% for(let i = 1; i <= totalPages; i++) { %>
                        <li class="page-item <%= currentPage === i ? 'active' : '' %>">
                            <a class="page-link" href="/admin/couponManagement?page=<%= i %>"><%= i %></a>
                        </li>
                    <% } %>
            
                    <% if (hasNextPage) { %>
                        <li class="page-item">
                            <a class="page-link" href="/admin/couponManagement?page=<%= nextPage %>" aria-label="Next">
                                <i class="fas fa-chevron-right"></i>
                            </a>
                        </li>
                    <% } %>
                </ul>
            </nav>
        </div>
    </div>
</div>
</main>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>