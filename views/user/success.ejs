<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Success</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .success-checkmark {
            animation: scale-up 0.5s ease-in-out;
        }

        .success-text {
            animation: fade-in 1s ease-in;
        }

        .action-buttons {
            animation: slide-up 0.7s ease-out;
        }

        @keyframes scale-up {
            0% { transform: scale(0); }
            70% { transform: scale(1.1); }
            100% { transform: scale(1); }
        }

        @keyframes fade-in {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        @keyframes slide-up {
            from { transform: translateY(50px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }

        .btn-custom {
            transition: transform 0.3s ease;
        }

        .btn-custom:hover {
            transform: translateY(-3px);
        }

        @media (max-width: 768px) {
            .success-container {
                width: 90% !important;
                padding: 1.5rem !important;
            }
            
            .success-checkmark {
                width: 80px !important;
                height: 80px !important;
            }

            .btn-custom {
                width: 100%;
                margin: 0.5rem 0 !important;
            }
        }
    </style>
</head>
<body class="bg-light">
    <div class="container min-vh-100 d-flex align-items-center justify-content-center">
        <div class="success-container bg-white rounded-4 shadow-sm p-5 text-center" style="width: 600px">
            <div class="success-checkmark mb-4">
                <svg width="100" height="100" viewBox="0 0 100 100" fill="none">
                    <circle cx="50" cy="50" r="45" stroke="#28a745" stroke-width="8"/>
                    <path d="M30 50L45 65L70 35" stroke="#28a745" stroke-width="8" stroke-linecap="round"/>
                </svg>
            </div>
            
            <div class="success-text">
                <h2 class="mb-3">Payment Successful!</h2>
                <p class="text-muted mb-4">Your transaction has been completed successfully. Thank you for your payment.</p>
                
                <div class="order-details mb-4">
                    <p class="mb-1">Order ID: <%= orderId %></p>
                    <p class="mb-1">Amount Paid: $<%= amount %></p>
                </div>
            </div>

            <div class="action-buttons d-flex flex-wrap justify-content-center gap-3">
                <a href="/home" class="btn btn-primary btn-lg btn-custom px-4">
                    Back to Home
                </a>
            </div>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>