<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Tactics</title>
    <link rel="icon" type="image/x-icon" href="/images/produts-tactics/LOGO/3.png">

    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Animate.css for Animations -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    
    <style>
        :root {
            --primary-dark: #121212;
            --secondary-light: #F5F5F5;
            --accent-blue: #007bff;
            --transition-speed: 0.3s;
        }

        * {
            box-sizing: border-box;
            transition: all var(--transition-speed) ease;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--secondary-light);
            color: var(--primary-dark);
            overflow-x: hidden;
        }

        .parallax-bg {
            background-attachment: fixed;
            background-position: center;
            background-repeat: no-repeat;
            background-size: cover;
        }

        .glass-card {
            background: rgba(255, 255, 255, 0.2);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            border: 1px solid rgba(255, 255, 255, 0.125);
            box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.1);
        }

        .hero-section {
            min-height: 100vh;
            display: flex;
            align-items: center;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            position: relative;
            overflow: hidden;
        }

        .hero-content {
            z-index: 2;
            position: relative;
        }

        .animated-underline {
            position: relative;
            display: inline-block;
        }

        .animated-underline::after {
            content: '';
            position: absolute;
            width: 0;
            height: 3px;
            bottom: -5px;
            left: 0;
            background-color: var(--accent-blue);
            transition: width 0.3s ease;
        }

        .animated-underline:hover::after {
            width: 100%;
        }

        .value-icon {
            font-size: 3rem;
            margin-bottom: 1rem;
            color: var(--accent-blue);
            transition: transform 0.4s ease;
        }

        .value-card:hover .value-icon {
            transform: rotate(360deg) scale(1.2);
        }

        .stats-number {
            font-weight: bold;
            color: var(--accent-blue);
        }

        @media (max-width: 768px) {
            .hero-section {
                min-height: 80vh;
                padding: 2rem 0;
            }
        }

        /* Advanced Responsive Typography */
        @media (max-width: 576px) {
            .display-4 { font-size: 2.5rem; }
            .display-6 { font-size: 1.5rem; }
        }


        <style>
    .stats-container {
        display: flex;
        justify-content: center;
        gap: 2rem;
    }

    .stat-item {
        text-align: center;
        perspective: 1000px;
    }

    .stats-number {
        font-size: 3rem;
        font-weight: bold;
        color: #007bff;
        position: relative;
        transition: all 0.5s ease;
        display: inline-block;
        transform-style: preserve-3d;
    }

    .stats-number::before {
        content: '';
        position: absolute;
        bottom: -10px;
        left: 0;
        width: 100%;
        height: 3px;
        /* background: linear-gradient(to right, #007bff, #00c6ff); */
        transform: scaleX(0);
        transition: transform 0.5s ease;
    }

    .stats-number.animated::before {
        transform: scaleX(1);
    }

    .stat-label {
        color: #6c757d;
        text-transform: uppercase;
        letter-spacing: 1px;
        margin-top: 0.5rem;
    }

    @keyframes numberPopIn {
        0% {
            opacity: 0;
            transform: scale(0.5) rotateY(-180deg);
        }
        100% {
            opacity: 1;
            transform: scale(1) rotateY(0);
        }
    }

    .stats-number.animated {
        animation: numberPopIn 1s cubic-bezier(0.25, 0.1, 0.25, 1) forwards;
    }

    /* Responsive Adjustments */
    @media (max-width: 768px) {
        .stats-container {
            flex-direction: column;
            align-items: center;
        }
        .stat-item {
            margin-bottom: 1rem;
        }
    }
    </style>
</head>
<body>

  
      <!-------------------------------------------------------------------------------------------------------------------------------------------------------------------------->
 <%- include('./layout/NAV.ejs') %>
 <!----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------->
 

    <!-- Hero Section with Parallax Effect -->
    <section class="hero-section text-center text-white position-relative parallax-bg">
        <div class="container hero-content">
            <div class="row justify-content-center">
                <div class="col-lg-10">
                    <h1 class="display-4 animate__animated animate__fadeInDown mb-4">
                        <span class="animated-underline">TACTICS</span>: Beyond Fashion
                    </h1>
                    <p class="lead animate__animated animate__fadeInUp">
                        We're not just designing clothes. We're crafting experiences that empower the contemporary Indian.
                    </p>
                </div>
            </div>
        </div>
    </section>

    <!-- Brand Values Section -->
    <section class="py-5 bg-white">
        <div class="container">
            <div class="text-center mb-5">
                <h2 class="display-6 animated-underline">Our Core Values</h2>
            </div>
            <div class="row g-4">
                <div class="col-md-4">
                    <div class="card glass-card p-4 text-center h-100 value-card">
                        <div class="value-icon">
                            <i class="fas fa-rocket"></i>
                        </div>
                        <h4>Innovation</h4>
                        <p>Pushing boundaries and exploring uncharted territories in design.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="card glass-card p-4 text-center h-100 value-card">
                        <div class="value-icon">
                            <i class="fas fa-heart"></i>
                        </div>
                        <h4>Empathy</h4>
                        <p>Understanding and designing for real people, not just trends.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="card glass-card p-4 text-center h-100 value-card">
                        <div class="value-icon">
                            <i class="fas fa-globe"></i>
                        </div>
                        <h4>Sustainability</h4>
                        <p>Committed to responsible fashion that respects our planet.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Statistics Section -->
    <section class="py-5 bg-light">
      <div class="container text-center py-5">
        <h2 class="display-6 mb-5">Our Journey So Far</h2>
        <div class="stats-container row g-4">
            <div class="col-6 col-md-3 stat-item">
                <div class="stats-number" data-target="7">0</div>
                <div class="stat-label">Years of Innovation</div>
            </div>
            <div class="col-6 col-md-3 stat-item">
                <div class="stats-number" data-target="250">0</div>
                <div class="stat-label">Team Members</div>
            </div>
            <div class="col-6 col-md-3 stat-item">
                <div class="stats-number" data-target="100" data-plus="+">0</div>
                <div class="stat-label">Products Sold (Lakhs)</div>
            </div>
            <div class="col-6 col-md-3 stat-item">
                <div class="stats-number" data-target="60" data-plus="+">0</div>
                <div class="stat-label">App Downloads (Lakhs)</div>
            </div>
        </div>
        </div>
    </section>

    
<!----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------->
    <!------footer------->
    <%- include('./layout/FOOTER.ejs') %>
      <!----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------->
      

    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        function animateCounters() {
            document.querySelectorAll('.stats-number').forEach(el => {
                const target = parseInt(el.getAttribute('data-target'));
                let current = 0;
                
                const updateCounter = () => {
                    const increment = target / 100;
                    if (current < target) {
                        current += increment;
                        el.textContent = Math.ceil(current);
                        requestAnimationFrame(updateCounter);
                    } else {
                        el.textContent = target;
                    }
                };
                
                updateCounter();
            });
        }

        window.addEventListener('scroll', function() {
            const statsSection = document.querySelector('.bg-light');
            const sectionTop = statsSection.getBoundingClientRect().top;
            const windowHeight = window.innerHeight;

            if (sectionTop < windowHeight * 0.75) {
                animateCounters();
                window.removeEventListener('scroll', arguments.callee);
            }
        });

        function createCounterAnimation() {
        const statsNumbers = document.querySelectorAll('.stats-number');
        
        const animateCounter = (element, target) => {
            let current = 0;
            const duration = 2000; // Total animation time in ms
            const increment = target / (duration / 16); // 16ms is roughly one frame

            const updateCounter = () => {
                current += increment;
                
                // Add plus sign for specific elements
                const displayValue = element.dataset.plus 
                    ? `${Math.min(Math.floor(current), target)}${element.dataset.plus}`
                    : Math.min(Math.floor(current), target);
                
                element.textContent = displayValue;

                if (current < target) {
                    requestAnimationFrame(updateCounter);
                } else {
                    element.textContent = element.dataset.plus 
                        ? `${target}${element.dataset.plus}` 
                        : target;
                    element.classList.add('animated');
                }
            };

            updateCounter();
        };

        const observerOptions = {
            threshold: 0.5
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    const target = parseInt(entry.target.dataset.target);
                    animateCounter(entry.target, target);
                    observer.unobserve(entry.target);
                }
            });
        }, observerOptions);

        statsNumbers.forEach(number => {
            observer.observe(number);
        });
    }

    // Initialize on DOM load
    document.addEventListener('DOMContentLoaded', createCounterAnimation);

    </script>
</body>
</html>